#ifndef SOLVER_HPP_
#define SOLVER_HPP_

void difference_recurrence(const char *file_path, const double* init, const double L, const double T,
                           const double DEL_X, const double DEL_T, const int PRINT_WIDTH = 500, const double FRAME_LEN = 0.2);

#endif